
// This is a workaround for a bug in the current version of Lucide React.
// The bug causes the build to fail when using dynamic imports with Lucide.
// By exporting all icons from a single file, we can avoid this issue.

export {
  ArrowRight,
  GraduationCap,
  Menu,
  ChevronLeft,
  FileQuestion,
  Timer,
  ArrowLeft,
  Check,
  XCircle,
  Clock,
  CheckCircle,
  Star,
  RotateCw,
  HelpCircle,
  X,
  Send,
  MessageSquare,
  Users,
  Rocket,
  Trophy,
  ChevronDown,
  CalendarWeek,
  Zap,
  ZapOff
} from 'lucide-react';
